let hello=function(name)
{
    return `hello and welcome ${name}`;
}
document.write(hello(1));
 
let sys=(name,age,roll)=>{
    return (`hello and welcome ${name}-${age}-${roll}`)
}
document.write(sys("system",1,2));